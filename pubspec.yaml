name: mealdb
description: A simple MealApp using TheMealDB API
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  cached_network_image: ^3.2.3
  provider: ^6.0.6
  url_launcher: ^6.1.7  # add this line


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
